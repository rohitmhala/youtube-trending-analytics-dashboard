# Local development (FastAPI running via: uvicorn main:app --reload)
VITE_API_BASE_URL=http://127.0.0.1:8000/api

# Production (replace with your deployed API Gateway URL in Phase 15)
# VITE_API_BASE_URL=https://xxxxxxxxxx.execute-api.ap-south-1.amazonaws.com/api
